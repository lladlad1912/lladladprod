[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["chmod +x mvnw", "./mvnw -DskipTests package"]

[start]
cmd = "java -Dspring.profiles.active=prod -jar target/blog-application-1.0.0.jar"
